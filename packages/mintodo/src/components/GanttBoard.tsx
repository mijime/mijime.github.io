export function GanttBoard() {
	return (
		<div data-testid="gantt-board" className="w-full h-full flex items-center justify-center">
			<span className="text-sm" style={{ color: "var(--mid)" }}>
				gantt (stub — replaced by Task 13)
			</span>
		</div>
	);
}
