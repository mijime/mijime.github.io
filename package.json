{
  "name": "mijime.github.io",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --cwd apps/main dev",
    "preview": "bun run --cwd apps/main preview",
    "build": "bun run --filter '*' build",
    "check": "bun run --filter '*' check",
    "format": "bun run --filter '*' format"
  },
  "type": "module",
  "workspaces": ["apps/*", "packages/*"]
}
